{
  "name": "if-only",
  "version": "2.0.0",
  "description": "A tiny react component that only renders children if a condition is met.",
  "main": "lib/If.js",
  "repository": "https://github.com/Drawbotics/if-only.git",
  "author": "Lorenzo Ruiz <lorenzo@drawbotics.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf lib/",
    "build:commonjs": "NODE_ENV=production babel src --out-dir lib/",
    "build": "npm run clean && npm run build:commonjs",
    "prepublish": "npm run build"
  },
  "peerDependencies": {
    "react": ">=16.0.0-0",
    "react-dom": ">=16.0.0-0"
  },
  "devDependencies": {
    "babel-cli": "^7.0.0-beta.3",
    "babel-core": "^7.0.0-beta.3",
    "babel-preset-env": "^7.0.0-beta.3",
    "babel-preset-react": "^7.0.0-beta.3",
    "rimraf": "^2.6.1"
  },
  "dependencies": {}
}
